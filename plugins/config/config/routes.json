{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "config.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/",
      "handler": "config.update",
      "config": {
        "policies": []
      }
    }
  ]
}
